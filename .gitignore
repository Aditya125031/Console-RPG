# --- Standard Build Artifacts (Ignore these) ---
build/
bin/
*.exe
*.o
*.obj

# --- IDE Settings (Ignore these) ---
.vscode/
.idea/

# --- Libraries and DLLs (General Rule: IGNORE) ---
# Ignore these by default so you don't commit your own build outputs accidentally
*.dll
*.lib
*.a

# --- EXCEPTIONS (Crucial for your team) ---
# Force git to TRACK everything inside the 'extern' folder,
# even if it matches the ignored types above.
!extern/**/